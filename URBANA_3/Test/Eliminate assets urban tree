var asset = "projects/mapbiomas-raisg/TRANSVERSALES/URBANO_3/clasificacion-ft"
var urbanocoll = ee.ImageCollection(asset);
print(urbanocoll)
var list = [
            // 70209,
            // 70108,
            70108
            ]
print(list)
var eliminate = urbanocoll.filter(ee.Filter.inList('region', list))
print(eliminate)
var list_eliminate = eliminate.reduceColumns(ee.Reducer.toList(), ['system:index']).get('list').getInfo();
print('list_eliminate',list_eliminate)

list_eliminate.forEach(
function (imageName) {
      print(imageName)
      var asset1 = asset +'/'+ imageName
      print(asset1)
      // ee.data.deleteAsset(asset1)
       
    }
)
